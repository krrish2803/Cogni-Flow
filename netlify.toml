[build]
  command = "rm -rf dist && mkdir -p dist && cp *.html dist/ && cp -R public dist/public"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://cogni-flow.onrender.com/api/:splat"
  status = 200
  force = true
