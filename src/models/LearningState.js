const mongoose = require('mongoose');
const conceptSchema = new mongoose.Schema({ name: String, score: { type: Number, min: 0, max: 100, default: 0 }, evidence: String }, { _id: false });
const schema = new mongoose.Schema({ userId: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, unique: true }, knownConcepts: [conceptSchema], weakConcepts: [conceptSchema], misconceptions: [{ concept: String, description: String, count: { type: Number, default: 1 } }], confidenceLevel: { type: Number, default: 50, min: 0, max: 100 }, masteryProgress: { type: Number, default: 0, min: 0, max: 100 }, lastPracticedTopic: String, repeatedErrorPatterns: [String], hintHistory: [{ topic: String, level: Number, createdAt: { type: Date, default: Date.now } }], microPracticeHistory: [{ practiceId: { type: mongoose.Schema.Types.ObjectId, ref: 'PracticeItem' }, score: Number, createdAt: { type: Date, default: Date.now } }], conceptGraph: { nodes: [mongoose.Schema.Types.Mixed], edges: [mongoose.Schema.Types.Mixed], reasoningSummary: String, updatedAt: Date } }, { timestamps: true });
module.exports = mongoose.model('LearningState', schema);
